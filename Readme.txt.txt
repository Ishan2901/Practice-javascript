Simple basic level projects on HTML, CSS and Javascript.

For running the webapps, 
1. Install VS Code
2. Install the live server extension by going to View->extensions
3. Run the server with all the changes .

The above applications have consumed various APIs provided by various providers . The API used is compiled under the readme file of the project itself.
